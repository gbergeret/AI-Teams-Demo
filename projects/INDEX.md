# Projects — index

The map of org-level projects (Executive team). Read this to see what's active; open
a project's folder only when the task needs it. Keep it current: move a project to
**Archived** when it closes. (Engineering build work is tracked on a ticket system,
not here.)

## Active
- _None yet._ Copy `projects/_TEMPLATE/` to `projects/<YYYY-MM-DD-slug>/` to start one.

## Archived
- _None yet._
